
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dev_user_rol_db
    username: alvaro
    password: alvaro

  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    locations: classpath:db/migration,classpath:dev/db/migration